[build]
builder = "NIXPACKS"

[nixpacks]
plan = '''
provides = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
'''
